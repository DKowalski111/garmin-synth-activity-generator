# No API keys required by default.
# Routing uses the OSRM public demo server (free, no key needed).
#
# Optional: set GOOGLE_MAPS_API_KEY to use Google Directions API instead of OSRM.
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
